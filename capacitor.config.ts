import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vilmarcabanero.tasktracker',
  appName: 'Task Tracker',
  webDir: 'dist'
};

export default config;
